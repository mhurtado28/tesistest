# tesistest

## Dirección circular (Random Forest)

Módulo `circular_direction.py` con utilidades sin/cos y métricas angulares.

Prueba comparativa RF (antes vs después):

```bash
python3 rf_direction_circular_test.py
```

Integración en el pipeline principal: sección **5.3.1** de `tesis_fisico_secuential.py`.